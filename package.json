{
  "name": "tripzo",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "vite": "^5.0.0"
  }
}
